using UnityEngine;
using UnityEngine.UI;

public class RecordEntity : MonoBehaviour
{

    public Text Name, Score;
    public Image record_bg;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
